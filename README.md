<div align="center">
  <img alt="Rex Liu Logo" src="public/avatar.png" width="100" />
</div>

<h1 align="center">
  Rex Liu - Insights
</h1>

<div align="center">
  <p>
    <strong>Writer and Builder in Crypto, AI, and Life.</strong>
  </p>
  <p>
    Be The Sovereign Individual You Want To See In The World.
  </p>
</div>

<div align="center">
  <p>
    <strong>在加密、人工智能和人生浪潮里，一边折腾一边记录的人。</strong>
  </p>
</div>

<br />

## About

This is the personal blog and digital garden of **Rex Liu**, built with **Astro v5** and **Tailwind CSS v4**. It serves as a platform to share insights on Cryptocurrency, Artificial Intelligence, and Life philosophy.

## 关于

这是 **Rex Liu** 的个人博客和数字花园，基于 **Astro v5** 和 **Tailwind CSS v4** 构建。这里主要分享关于加密货币、人工智能以及人生哲学的思考与感悟。

---

## Key Features

- **High Performance**: Built on Astro v5, ensuring blazing fast load times.
- **Modern Styling**: Styled with Tailwind CSS v4 for a sleek, responsive design.
- **Content Focused**: Optimized for long-form content (Essays & Notes) using MDX.
- **Activity Graph**: GitHub-style heatmap displaying GitHub commits, blog posts, and Flomo notes.
- **SEO Friendly**: Automatic sitemaps, robots.txt, and semantic HTML.
- **Dark/Light Mode**: Seamless theme switching for better reading experience.

## 主要功能

- **高性能**: 基于 Astro v5 构建，确保极速加载体验。
- **现代化样式**: 使用 Tailwind CSS v4 设计，界面简洁响应式。
- **专注内容**: 针对长文（文章与笔记）优化的 MDX 支持。
- **活动热力图**: GitHub 风格热力图，整合展示 GitHub 提交、博客文章和 Flomo 笔记。
- **SEO 友好**: 自动生成站点地图、robots.txt 和语义化 HTML。
- **深色/浅色模式**: 无缝切换主题，提供更好的阅读体验。

---

## Quick Start

### Prerequisites
- Node.js (v18 or higher)
- pnpm (recommended)

### Installation

```bash
# Clone the repository
git clone https://github.com/rexliu1912/rexliu-insights.git

# Enter the directory
cd rexliu-insights

# Install dependencies
pnpm install
```

### Development

```bash
# Start local development server
pnpm dev
```
Open `http://localhost:3000` in your browser.

## 快速开始

### 前置要求
- Node.js (v18 或更高版本)
- pnpm (推荐)

### 安装

```bash
# 克隆仓库
git clone https://github.com/rexliu1912/rexliu-insights.git

# 进入目录
cd rexliu-insights

# 安装依赖
pnpm install
```

### 开发

```bash
# 启动本地开发服务器
pnpm dev
```
在浏览器中打开 `http://localhost:3000`。

---

## Commands

| Command | Description |
| :--- | :--- |
| `pnpm install` | Installs dependencies <br> 安装依赖 |
| `pnpm dev` | Starts local dev server <br> 启动本地开发服务器 |
| `pnpm build` | Build your production site to `./dist/` <br> 构建生产环境代码到 `./dist/` |
| `pnpm preview` | Preview production build locally <br> 本地预览构建结果 |
| `pnpm check` | Run type checking and linter <br> 运行类型检查和代码校验 |
| `pnpm sync` | Generate types for content collections <br> 同步内容集合类型 |

## 常用命令

| 命令 | 描述 |
| :--- | :--- |
| `pnpm install` | Installs dependencies <br> 安装依赖 |
| `pnpm dev` | Starts local dev server <br> 启动本地开发服务器 |
| `pnpm build` | Build your production site to `./dist/` <br> 构建生产环境代码到 `./dist/` |
| `pnpm preview` | Preview production build locally <br> 本地预览构建结果 |
| `pnpm check` | Run type checking and linter <br> 运行类型检查和代码校验 |
| `pnpm sync` | Generate types for content collections <br> 同步内容集合类型 |

---

## Project Structure

- `src/pages`: Website pages (Home, Blog, etc.)
- `src/content`: Markdown/MDX content (Posts, Notes)
- `src/components`: Reusable UI components
- `src/layouts`: Page layouts
- `src/styles`: Global styles and Tailwind config
- `public`: Static assets (Images, Fonts)

## 项目结构

- `src/pages`: 网站页面（首页、博客等）
- `src/content`: Markdown/MDX 内容（文章、笔记）
- `src/components`: 可复用的 UI 组件
- `src/layouts`: 页面布局
- `src/styles`: 全局样式和 Tailwind 配置
- `public`: 静态资源（图片、字体）

---

## License

MIT
